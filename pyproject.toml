[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "droidjoy-server"
version = "0.1.0"
description = "A custom Linux server for the DroidJoy Android gamepad app"
requires-python = ">=3.8"
dependencies = [
    "evdev>=1.7.0"
]
